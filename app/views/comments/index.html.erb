<% if @comments.empty? %>
	<h1><%= "Еще никто не писал :/" %></h1>
<% else %>
	<% for c in @comments %>
		<%= render :partial => 'comments/comment_index', :locals => { :c => c } %>
	<% end %>
<% end %>